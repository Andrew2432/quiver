query BlogSearchPosts($query: String!, $authorSlug: String!) {
  articles(where: { _q: $query, author: { slug: $authorSlug } }) {
    ...articleMetaParts
    ...imageParts
    author {
      ...authorParts
    }
    category {
      ...categoryParts
    }
  }
}
