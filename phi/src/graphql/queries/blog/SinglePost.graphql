query SinglePost($slug: String!) {
  articles(where: { slug: $slug }) {
    ...articleParts
    ...imageParts
    author {
      ...authorParts
    }
    previous {
      ...articlePreviousOrNextParts
    }
    next {
      ...articlePreviousOrNextParts
    }
  }
}
