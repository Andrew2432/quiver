query BlogPaginatedPosts($offset: Int!, $limit: Int!) {
  articles(start: $offset, limit: $limit) {
    ...articleMetaParts
    ...imageParts
    author {
      ...authorParts
    }
    category {
      ...categoryParts
    }
  }

  articlesConnection {
    aggregate {
      count
    }
  }
}
