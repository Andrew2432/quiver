query BlogSearchPosts($query: String!) {
  articles(where: { _q: $query }) {
    ...articleMetaParts
    ...imageParts
    author {
      ...authorParts
    }
    category {
      ...categoryParts
    }
  }
}
