query BlogCategoriesPosts($slug: String!) {
  categories(where: { slug: $slug }) {
    ...blogCategoriesParts
    articles {
      ...articleMetaParts
      ...imageParts
      author {
        ...authorParts
      }
      category {
        ...categoryParts
      }
    }
  }
}
